linters:
  enable:
    - gofmt
    - golint
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - ineffassign
    - unused
    - misspell

run:
  deadline: 5m

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gosec

linters-settings:
  errcheck:
    check-type-assertions: true
  govet:
    check-shadowing: true
  golint:
    min-confidence: 0